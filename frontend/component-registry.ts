/**
 * Angular frontend stub — expand under extensions/ for full Angular CLI apps.
 * Selector patterns match agent-emitted components.
 */
export const UI_AGENT_SELECTORS = [
  'app-button',
  'app-data-table',
  'app-card',
] as const;
